# タスク追跡 — armor-arena

**2026-07-28 に fable-playground から移管(以後の kouki/ARMOR ARENA 開発の正本はこのリポ)。**
完了したら `[x]` にして「完了メモ(日付・commit)」を1行残す。
2026-07-25 以前の制作記・仕様(契約)・git 履歴は
[fable-playground の making.md](https://github.com/d3j/fable-playground/blob/main/promo/2026-07-05-kouki/making.md) にある。
このリポでの表記: 本番=`public/`・開発版=`public/dev/`(旧 kouki / kouki-dev)。検証ハーネスは `tools/harness/`。

---

## 第29作「鋼機工廠」(kouki) — St2: 観戦3DをThree.jsへ移行

**背景**: 旧・自作ソフトウェアラスタライザ `public/r3d.js`(約2200行)から Three.js への段階移行。
描画方式のみ差し替え=**シム挙動不変(REPLAY_V は上げない)**。歩容/IK は純関数 `computeMechPose` に抽出し
ソフト版(`r3d.js`)と Three版(`r3d-three.js`)が同一の真実を共有。開発は `public/dev/` で行い各段レビュー。
既定=Three、`?soft=1` で旧ソフト版のA/B比較。Three初期化失敗時は自動でソフト版へ(常にgreen)。

**共通の掟**:
- 本番リリースは `r3d.js` / `r3d-three.js` / `game.js` を **手動コピー**(release-sim.mjs は sim/parts/fields 専用=使わない。
  REPLAY_V は据え置き)。`vendor/three.module.min.js` `three.core.min.js` も本番へ同梱。
- 歩容/動作を変えたら **物理ハーネスで検証**: `node tools/harness/gait-harness.mjs [--phys]`
  (`poseWorld`/`computeMechPose` を使い plantError/接地スリップ/重心マージン/cannon-es倒れテストを assert)。
- 「かっこよさ」の演出(カメラ・アニメ・撃破ラグドール)は**描画側**なら決定論リスクゼロ(LESSONS「描画側=安全/シム側=クロス環境FP発散」)。

### 完了
- [x] **第1段=二脚**: Three導入(buildless)・剛体パーツ木・motions→ボーン回転・ライティング+接地シャドウ・カメラdirector再利用・
  空/グリッド/フォグ。(2026-07-19, commit fce2c30)
- [x] **第1段レビュー反映**: 歩容の後退/横移動/重心を物理修正(横方向IK外転・接地脚フル到達・重心横シフト)+
  歩容物理ハーネス(cannon-es)導入。二脚 ALL GREEN。(2026-07-20, commit 3836059)
- [x] **プレビュー修正**: 歩行↔地面連動(機体を実際にワールド移動+カメラ周回追従)+grid-follow(無限地面)+
  フレームアウト修正(手動カメラ時は scaleScene を無効化=機体とカメラの座標系を一致)。(2026-07-20〜21, commit 0c7d142)
- [x] **第2段=四脚**: ①ハーネスに四脚の足接地計測を整備(poseWorld に toe=接地パーツ底面中心を追加。
  脛centroidの計測バイアスを解消したら運動学は既に正しく即緑)→四脚を緑判定ゲートに追加・ALL GREEN
  ②膝向きを前脚=前折れ/後脚=後折れのX字ミラーに調整(見え方のみ・IK到達不変)。Three/ソフト両経路+
  戦場TR-01決着周回を実機確認・console 0。(2026-07-24)
- [x] **第1+2段を本番リリース**: r3d.js/r3d-three.js/game.js(DEV=false戻し)手動コピー+vendor同梱。
  シム3ファイルはdev==本番=REPLAY_V=3据置・worker再デプロイ不要。check-freeze OK・ハーネス --dir . ALL GREEN・
  実機確認済。public/dev は第3段作業場として残置。(2026-07-24)

### レビューOK・本番昇格待ち(開発版 public/dev)
- [x] **動作モーションの拡充**(人間提案 2026-07-21): 4点とも public/dev に実装済。人間レビューOK
  (2026-07-24 工廠プレビュー確認)→ **本番昇格済**(2026-07-24: r3d.js/game.js 手動コピー・DEV=false戻し・
  REPLAY_V=3据置。check-freeze OK・ハーネス --dir . ALL GREEN・実機Three/soft両経路 console 0)。
  すべて描画側=computeMechPose への純関数加算(シム非改変・REPLAY_V据置)。工廠プレビューに
  アクション実演(juke→flinch→右腕攻撃→低速歩行→左腕攻撃の20s周期)を追加=戦闘を待たず確認可。
  ハーネス ALL GREEN・数値プローブ12項目 OK・Codex correctness 3巡収束。(2026-07-24)
  - [x] **回避(juke)**: dodge イベントで横っ飛びスウェイ(足は planted のまま上体でかわす)。
  - [x] **被弾(flinch/stagger)**: hit の押し込み方向へ上体のけぞり/よろけ+膝折れ(dmg比例)。hazard も小よろけ。
  - [x] **射撃/白兵の follow-through**: 攻撃窓 0.65→0.9s。射撃=全身反動+戻りの余韻、白兵=踏み込み前傾+前進シフト+振り抜き。
  - [x] **低速移動**: 低magほど歩幅+60%/接地デューティ+0.14(2乗カーブ=旋回歩行の中速域に効かせない)。位相は step/stride のその場積分へ。

### 完了(続き)
- [x] **第3段=残り全要素の移植**(Three版へ): ①脚種の見え(逆関節は planted 歩容化+接地修正=ハーネス
  緑判定入り・toe方式) ②8武装の攻撃演出・弾・爆風・マズルフラッシュ・砂煙(演出フェイス生成関数を
  r3d.js の module スコープへ純移動し両レンダラ共有) ③地形(泥/岩/沼) ④撃破余韻+敵機マーカーHUD
  ⑤リプレイ整合。**人間レビューOK(2026-07-24)→ 旧ソフトレンダラ(createR3D)撤去済**(?soft=1 は
  無視され Three のみ。WebGL不可端末は3Dだけ無効化の劣化継続)。**本番昇格はしない(人間指示)=
  public/dev のみ**。詳細 making.md「■St2 第3段」。(2026-07-24)
### レビューOK・本番昇格待ち(開発版 public/dev)(続き)
- [x] **コックピットビュー**(人間提案 2026-07-21): public/dev に実装済・**人間レビューOK(2026-07-24「動作確認OK。かっこいいね」)**。
  本番昇格は第3段以降と同様に人間判断待ち。
  中距離以遠(distEMA≥60・ショット開始時ラッチ)で SLOT_POV をコックピット目線に発展: コクピット高+前方
  オフセット+歩行スウェイ・POV専用尺2.2〜3.2s・tau0.12・照準リング(遮蔽=橙破線+距離読み)+計器枠
  (カメラ子の全画面スプライト)・自機メッシュ全hide。**カメラ/スクリーン装飾のみ=シム非改変(REPLAY_V=3据置)**。
  ハーネス --phys ALL GREEN・check-freeze OK・実機 console 0・Codex correctness 冷読み Med2 → 2件採用済。
  詳細 making.md「■St2 コックピットビュー」。(2026-07-24)
- [x] **機体鑑賞モード**(2026-07-26 別セッションが public/dev に実装): 工廠プレビュー直下の
  「⛶ 機体鑑賞」ボタン→専用画面 `data-screen="viewer"`。移動/歩調/旋回/動作/カメラ/構成送りを全ボタン操作、
  姿勢は戦闘と同じ computeMechPose。previewLoop→previewTick 化を含む。※未コミットWIPだったものを 2026-07-28 の
  移設コミット(da0eb64)が同梱して公開済み。実機確認=viewer 表示・canvas 描画・console 0(2026-07-28)。
  **人間レビューOK(2026-07-29「動作確認OK」)**。**本番昇格済(2026-07-29 commit 7d6a0d2・St3 と同時)**。
- [x] **鑑賞モードの構成送りが出撃機体(S.current)を書き換える**(2026-07-28 発見)→
  **修正済(2026-07-29 commit 04efd22・同日 7d6a0d2 で本番昇格)**: 鑑賞用ビルドを S.current のコピーに分離(ui.js viewerBuild、
  開くたびに工廠の現構成から作り直し・保存なし・持ち帰りなし。game.js は onDeriveStats=表示専用の
  ステータス再計算を追加し viewerTick/vwFire はコピーを描画)。実機確認: 鑑賞で予算超過3,450Cに送っても
  工廠/出撃機体は2,650Cのまま・localStorage 書込ゼロ・console エラー0。harness(dev) ALL GREEN・
  gait-harness --phys ALL GREEN。ついで掃除: r3d.js GEN_STYLES 残留 size コメント修正・wheels:1 の0除算ガード・
  harness に「{LEGA}〜{WB}は start_build 限定」チェック追加(緑)。
- [x] **St3=機体の作り込み**(2026-07-28 public/dev に実装・**人間レビューOK 2026-07-29「動作確認OK」**・
  **本番昇格済 2026-07-29 commit 7d6a0d2=REPLAY_V 3→4 リリースと同時**)。参考の文法:
  メックウォーリア=重量級のスラブ装甲/リベット、アーマード・コア=軽量機のウエスト絞り/肩ブースタ/センサ記号、
  バーチャロン=発光部のヒーローシルエット。4本柱:
  ① **パーツid別の固有形状**(r3d.js mechMesh): フレーム8種=胴プロポーション+胸部意匠+頭部センサ
  (バイザー/単眼/双眼/旗甲クレスト+アンテナ)、脚14種=同一運動学のまま肉付け差(疾風=脚ブースタ/
  堅牢=膝装甲/守宮=低重心フェンダー/跳兵=ピストン/亀甲=甲羅天板/城塞=サイドスカート/風車=スポーク/
  疾駆=フェンダー/浮舟=舷側フロート等)、武器18種=id別レシピ(長銃身+ブレーキ/回転3連銃身/冷却リング/
  蓄電器/ドラム弾倉/発射管×n/金属厚刃 等)、動力炉7種=背部ユニット(タンク2本/タービン/発光コア等)、
  装甲7種=増加装甲オーバーレイ+肩形状(ERAブロック/段積み/流体シーム発光等)。**IK/歩容/演出の契約は不変**
  (legU/legL/foot/toe/spur 名と pivot・role 群)。未知idは kind/tier 既定形に落ちる。
  ② **プロシージャルテクスチャ**(r3d-three.js): 機体=金属パネル柄(白ベース×color乗算・面法線ドミナント軸の
  平面投影UV)、地面=テーマ別の土テクスチャ(タイル80mにスナップ追従=流れ防止)、岩/茨=節理ノイズ
  (頂点色×白ベース乗算)。外部アセットなし・シード固定・シェーダプリウォーム構成も更新。
  ③ **新パーツ9種**(parts.js 追記・既存kindのみ=sim非改変): fr8ウツセミ換装殻/lg13二脚・野分/
  lg14四脚・岩戸/gn7渦潮炉/ar7流体装甲/wp15双連ビーム/wp16重散弾/wp17徹甲ライフル/wp18大蛇ミサイル。
  harness に新パーツ使用の lancer/bombard アーキタイプを追加し総当たりゲート緑(勝率 max64%/min28%・引分10.2%)。
  **リリース時は REPLAY_V 3→4 が必要**(ハザード実装と同じ v4 に同梱可)。
  ④ **パーツ連動実況**(voice-lines/voice/game): 開幕ロードアウト紹介(start_build: {LEGA}{LEGB}{WA}{WB})、
  脚種別の回避/脚部破損バンク(dodge@hover 等5種/pbreak@hover 等5種)、遠距離発砲 fire@long({DIST})。
  narrate に buildA/buildB を渡す(未指定なら従来動作)。セリフ+63本=総657本。
  検証: harness.mjs(dev) ALL GREEN・gait-harness --phys ALL GREEN・mechMesh 全id掃引994構成 重複/親解決エラー0・
  poseWorld 全脚種 歩行+撃破スイープ OK・実機 Three 描画+console 0(playwright)。
- [x] **コックピットHUD=ワイド画面で3D・レーダー・実況を同時表示**(人間要望 2026-07-29・public/dev に実装
  =commit febe2f8・**本番昇格済 2026-07-30(人間指示「本番リリースして」)**): 幅1100px以上でバトル画面を
  タブ切替から「3D全面+計器オーバーレイ」へ自動切替。
  配置=上段: TGT-A(左)/CONTROL(中)/TGT-B(右)、下段: レーダーPiP(左下・不透明計器)/実況フィード
  (右下・半透明ガラス+上端フェード+細スクロールバー)、戦果は画面中央のガラスパネル。
  実装: ui.js=matchMedia(min-width:1100px)で .cockpit を付け外し(setBattleTab が cockpit を見て
  全ビュー常時表示に一元化=AUTOデモのタブ自動クリックでも消えない)、game.js=cockpit 時は
  radar+r3d を毎フレーム両方描画(通常はアクティブタブのみ)、style.css=.cockpit 一式(約70行)。
  **プレゼン層のみ=シム非改変・REPLAY_V据置**。1100px未満(モバイル/中幅)は従来レイアウトのまま。
  実機検証(playwright): 1300/1440px コックピット表示・900px 従来・390px モバイル・戦闘中の
  幅リサイズ往復・AUTOタブ自動切替通過・戦果パネル(WIN)中央表示、いずれも console エラー0。
  昇格(2026-07-30): ui.js/game.js/style.css を public/ へ手動コピー・DEV=false 戻し。
  check-freeze OK(v4==live)・harness(本番) ALL GREEN・本番ファイル実機コックピット表示OK
  (console のCORSエラーは localhost→Worker の既知挙動で本件無関係)。
- [x] **St4=外装と地形の質感・遠景・市街戦マップ**(人間要望 2026-07-31「ロボパーツや地形のテクスチャが
  しょぼい/パーツごとにテクスチャも変える/フィールドの遠景/市街戦マップ」・**public/dev に実装**。
  人間レビュー待ち → 本番昇格は下記「昇格手順」)。4本柱すべて**描画側のみ**だが、④で戦場を1つ足すため
  **リリース時に REPLAY_V 4→5 が必要**(fields.js が凍結トリオに入っているため。掟2・3)。
  - ① **質感を3枚組に**(新規 `public/dev/tex.js` 約1000行): 1クラス=albedo+法線(高さcanvasからSobel)+
    roughness/metalness パック(G=rough / B=metal)。すべて canvas 2D と決定論ノイズで生成し外部アセットゼロ・
    シード固定。**環境マップ**(テーマの空/太陽/地面を equirect に描き PMREM でぼかす)を入れたので
    metalness を 0.12 → 0.3〜0.9 の実用域へ上げられた(St3 が抑えていた理由=env map 不在で金属が黒く潰れる)。
    レンダラに **ACES トーンマッピング**(exposure 1.18)。演出/HUD は toneMapped:false で彩度を守る。
  - ② **パーツごとに違う質感**(13クラス): panel(塗装パネル)/plate(厚板+溶接ビード)/cast(鋳鉄の巣穴)/
    weave(繊維の綾織)/fluid(艶と流路)/era(反応装甲レンガ+注意帯)/gunmetal(旋盤目+ナーリング+発砲焼け)/
    blade(研磨目+刃文)/drill(螺旋刃)/rubber(履板の山形)/grille(ルーバー)/thermal(放熱フィン+熱酸化+すす)/
    pod(危険帯+ステンシル)。割当は **makePart 名の前方一致**(呼び出し300箇所に触らない)で、
    名前だけでは決まらない肩だけ `opts.mat` を明示(装甲id別に鋳肌/厚板/織り/艶/ERA)。
    テクセル密度もクラス別=同じ機体の中で「材質が違う」ことが目で分かる。全構成掃引98,784件で
    ユニーク104パーツ名の割当を確認。
  - ③ **遠景**(`distantSceneryFaces`): 地平に3層のシルエット(山稜 or 市街のスカイライン)。
    フォグ域(220〜900)の外なので **unlit+fog無効**にし、空気遠近法(遠い層ほど地平の空色へ寄せる)を
    手で焼く。新規の遠景/装飾の頂点色だけ **sRGB→リニア変換**する(Three の頂点色は作業色空間扱い=
    16進をそのまま入れると約2倍明るい。既存の障害物/演出の配色はその前提で調整済みなので触らない)。
  - ④ **市街戦マップ「崩落市街」(shigai)**: 十字の大通り=最短だが遮蔽ゼロ / 街区の路地=遠いが必ず遮蔽、
    という射線のジレンマ。大通りの唯一の盾は交差点の不壊塔、南北端に崩落瓦礫(鉄筋)・東西端に冠水。
    外周の高層2棟は不壊。見た目は `scene.field` で分岐(**当たり判定は共通・シムは戦場idを知らない**):
    wall=鉄筋コンクリのビル(不壊=セットバック付き高層+航空障害灯 / 損傷=亀裂と焦げ / 全壊=瓦礫の山と
    折れた壁+露出鉄筋)、spike=崩落高架の瓦礫、mud=冠水した街路。さらに **render-only の装飾層**
    (`fieldDecorFaces`)で舗装・縁石・区画線・横断歩道・街灯・信号・電線・廃車・瓦礫・**場外の街**を置く。
    地面テクスチャも市街だけ舗装へ切替、フォグ色も都市の中性色へ。
  - ⑤ **近接カット**(`updateObstacleCull`): カメラと狙点の間に入りカメラより高い遮蔽物を 0.1s で
    透かして消す。高層ビルで画面が壁一枚になるのを防ぐ(岩柱の戦場でも効く)。
  - 検証: harness(dev) ALL GREEN(**FIELD_IDS に shigai 追記**・勝率 max64%/min27%・引分10.3%=既存と同水準)/
    hazard-probe ALL GREEN(shigai は wheel が冠水を回避=泥0.0s vs biped1.7s)/ gait-harness --phys ALL GREEN /
    スポーン実測4000シード: フォールバック0%・埋まりスポーン0件・最小クリアランス26m(既存戦場より安全)/
    面生成プローブ NaN 0件(全戦場×破壊状態×テーマ)/ 実機 playwright: 1440px・390px・演習/闘技場テーマ・
    戦闘完走(崩落市街 WIN 55.5s)いずれも **console エラー0**、フレーム間隔 median 16.7ms / max 18.7ms(=60fps維持)。
  - 実機で見つけて直した見た目の不具合(検証スニペット `tools/harness/freecam-snippet.js` を新設。
    観戦カメラでは狙った場所を映せないので、手動カメラで任意座標を覗く): 冠水が道路パッチに
    塗り潰されて消えていた(水面のyを装飾より上へ)/ 縁石・支柱に岩肌テクスチャが9倍拡大されて
    巨大なジグザグになっていた(小物用に concrete クラスを新設)/ 遠景ビルが白い書割に見えていた(色空間)/
    ステンシル文字が全パーツで繰り返されて汚れに見えていた(panel から外し pod のみに)/
    塗装剥げが泥の染みに見えていた(中を明るい素地色へ反転)/ 地面の低周波ムラがタイルの水玉として
    見えていた(中周波・低コントラストへ)/ 発光パーツがトーンマッピングで白飛びして色の記号が消えていた
    (emissiveIntensity 0.9→0.6)。ついで PCFSoftShadowMap(three r185 で非推奨・毎生成で警告)を
    PCFShadowMap へ。
  - **クロスレビュー(Codex×Claude 並列)で直した2件**(2026-07-31。重複ゼロ・互いに片方だけが発見):
    ⑴ **法線マップが albedo と V 方向にズレていた**(Claude): `DataTexture` の既定は
    flipY=false/Nearest/mipmapなしで、albedo・ORM の `CanvasTexture`(flipY=true/Linear+Mipmap)と食い違い、
    法線だけが V 方向に鏡像=「色はここ・凹凸は上下逆の位置」になっていた(凹凸の向き自体は
    flipY=false 前提として正しくエンコードされていた=不具合は位置ズレの方)。DataTexture に
    flipY=true を立てても効かない(UNPACK_FLIP_Y_WEBGL は ArrayBufferView に適用されない=WebGL仕様)ので、
    法線も canvas 経由に統一。**canvas 経由にすると V 軸の向きが変わるので、同時に緑チャンネルの
    符号反転も必要**(片方だけ直すと鋲が凹みになる)。three は OpenGL/Y-up 規約(同梱バンドルの
    getTangentFrame で確認)。修正後リベットが凸・継ぎ目が溝として読める。副次効果で mipmap/anisotropy が
    有効になり地面遠方のちらつきも解消(DataTexture の Nearest では three が anisotropy を設定しない)。
    ⑵ **ハーネスが既定ターゲットで嘘のカバレッジを出していた**(Codex): `FIELD_IDS` に 'shigai' を
    ハードコードしたため既定の `KOUKI_DIR=../../public`(本番)では `getField` が黙って plain へ
    フォールバックし plain の二重計上に。`FIELDS` から導出する形へ変更+実行時に対象と戦場一覧を出力。
  - **参考(未対応・次セッション以降の候補)**: ①初回フレームのテクスチャ生成コスト(純JSだけで76ms、
    canvas描画と getImageData が上乗せ。既存のシェーダプリウォームと同じ理由で先出しの余地あり。
    レンダラ3インスタンス=工廠/鑑賞/戦闘 ごとに地面512²とPMREMが重複生成、アスファルトも2組)。
    ※過去に記録した「max 18.7ms」は生成フレームを捕まえていない計測なので訂正。
    ②空ドームは生の ShaderMaterial で tonemapping/colorspace の include が無く、ACES が空だけに
    掛からない(フォグ色と空の下端の関係が St4 前と変わる)。
    ③~~`harness-budget.mjs:19` だけ戦場をハードコードのまま~~ → **修正済(2026-08-01・小障害物と同じコミット)**:
    `FIELDS.map(f=>f.id)` 化+対象と戦場一覧を出力。dev 7戦場・本番 6戦場を回すようになった
    (ibara/shigai が入ったぶん勝率のベースラインは動く。全ゲート緑は実測確認)。
    ④`harness.mjs` は N=20 が戦場数の倍数でないため戦場ごとの試合数に偏りが出る(本番なら plain/sekichu が
    288戦・他216戦)。並び順を FIELDS 由来に変えた影響でベースライン数値が動くので、docs に残る
    過去の勝率値との直接比較はできない(全ゲートの反転は無し=実測で確認済み)。
  - ⚠ **昇格前に `make-dev.mjs` を流さないこと**: make-dev は public/ から public/dev を再生成するため、
    本番にまだ無い `tex.js` を含む St4 一式が消える(git からは復旧できるが作業が飛ぶ)。
    dev を作り直したい場合は先に下記の昇格手順⑴で描画側を本番へ入れてから。
  - **昇格手順**(人間承認後。**下記「小障害物」と同梱で v5 にする=人間判断 2026-08-01**。
    描画側のコピー対象に `radar.js` が加わる点に注意):
    ⚠ **⑴と⑵は必ず同じコミットにする**。v5 の `r3d.js` は `fields.js` から `CLIMB_TOP_FRAC` を
    import するので、`r3d.js` だけ先に本番へ入って main が push されると、本番の `fields.js` に
    その export が無く **ESM のリンクエラーで本番が白画面**になる(pages.yml は push で即デプロイ)。
    ⑴ 描画側 `tex.js`(新規)・`r3d.js`・`r3d-three.js`・`radar.js`・`game.js`(DEV=false 戻し)を
    public/ へ手動コピー ⑵ `node tools/harness/release-sim.mjs` で sim.js/fields.js を本番へ入れ
    **REPLAY_V 4→5**+`sims/v5/` 凍結 ⑶ `replay.js` の FIELD_CODES 末尾へ `'shigai'` 追記(掟2。並べ替え禁止)
    ⑷ **`public/dev/replay.js` を本番と同期する**(REPLAY_V と FIELD_CODES の両方)。release-sim.mjs は
    本番の replay.js しか +1 しないので、放置すると dev だけ版番号が取り残され、**dev が発行する
    リプレイコードが v4 スタンプのまま v5 挙動で作られる**(再生すると別の試合になる)。昇格後に
    make-dev.mjs で dev を作り直すなら自動的に揃うので、そちらでもよい(Codex×Claude レビュー 2026-08-01)。
    ⑸ 旧 v4 リプレイコードの再生互換を実測 ⑹ `workers/kouki`(fable-kouki)を再デプロイ(サーバ権威シムも
    fields.js を持つため) ⑺ check-freeze・harness・実機確認。
  - **昇格で一緒に変わるもの**(仕様として承知しておく点): `dailyEnemy` の戦場プールが 5→6 になるため
    **全日付の日替わり演習の戦場が入れ替わる**(末尾に足しても `fpool.length` が変わるので回避不能。
    リプレイは fieldId を明示保存するので再現性は無事)。出撃画面の戦場選択リストの並び順も変わる
    (`fields.js` の挿入位置が haikyo の前=index 5 のため。見た目のみ)。
- [x] **小障害物の当たり判定=地形に表情を**(人間要望 2026-07-31・**public/dev に実装 2026-08-01**・
  人間レビュー待ち)。**リリースは St4 と同梱で REPLAY_V 4→5(人間判断 2026-08-01)**=worker 再デプロイ1回・
  `sims/v5/` 凍結1世代・FIELD_CODES 追記1回で済ませる。
  - **設計の芯 = 新 kind `rubble`(踏破可能な小障害物)+ 属性 `h`(足場の標高m)**。既存 kind に高さを足すのでは
    なく kind を足した(wall/mud/spike は「止める・遅くする・削る」で意味が確定していて、
    「乗る」を混ぜると全消費者の分岐が壊れる)。壁と違い**射線は切らない**(数mの高低差では
    12〜20mの壁越しは見えない=遮蔽の勘定を変えないほうが観戦者の理解と一致する)。
  - **① 昇格の線引き**(fields.js 冒頭に正本を書いた): シムの障害物は円ひとつなので、昇格の条件は
    ⑴円1個で素直に表せる ⑵半径が MECH_R=2.2m と同オーダー以上(実務上 r≥8m)——の2つだけ。
    細長い縁石・区画線・電線や r≈1.2m の支柱は満たさない。**満たさないものは昇格させず、
    装飾側を「機体がすり抜けても嘘に見えない高さ」へ落とす**方針にした(崩落市街なので
    街灯22/信号4/電柱7=33本すべて「根元から折れて路面に倒れた姿」に作り替え。灯体は路面でまだ点いている)。
    結果、**「機体可動域(shapeClamp の 20..980)に立つ実体は必ずシムが知っている」**が不変条件になった
    (実測: 20..980 内の装飾頂点で y>2 は0件)。市街に昇格したのは 大通りの崩落塊4・廃車6・崩れた縁石8 の計18。
  - **② 移動/回避ペナルティ**: `CLIMB_FACTOR`(rubble内の速度倍率)を脚種別に。泥とはわざと得意不得意を
    入れ替えた: 履帯 泥0.65/段差0.86・逆関節 泥0.6/段差0.82・車輪 泥0.35/段差0.45(両方最弱)・
    hover は免疫。「泥の戦場に強い脚」と「瓦礫の戦場に強い脚」が別になる。
  - **③ 乗り越え中は当たり判定が変わる**: 露出率 e=h/4.0(機体全高)として
    **回避×(1−0.42e) / 命中+0.10e**。人間の言葉「丘の頂点は露出が大きい」を
    「躱せないが当てやすい」の一対に翻訳した。実測で 高所の回避率12.5% vs 地上22.6%。
  - **ハザードAIの第3の選択肢**: 従来の「渡る/迂回」に「乗る」を追加。
    踏破係数+意欲+**見晴らしの価値**(撃ち合える距離で遠距離武器が使えるときだけ)−**露出の損**
    (回避が持ち味の機体ほど大きい)≥0.85 で乗る。rng は一切増やしていない(判断は機体状態のみ=決定論不変)。
  - **④ 全戦場に配置**: plain=中央の塚(唯一の高所。rubbleは射線を切らないので「遮蔽なしの正面決戦」は保つ)/
    sekichu=砕けた石柱の裾と回廊端の崩落塊 / deitan=泥炭の堆積(泥円とは重ねない=二重減速を避ける)/
    crater=中央岩から崩れた岩塊(対角の安全路にも代償)/ ibara=中央岩の脇の段差 / haikyo=廃材の山 /
    shigai=上記18。**スポーン円(中心から半径220m)の上には置かない**——8回試行の角度窓を潰して
    フォールバック率が上がるため(crater で 1.45%→3.17% に悪化させてしまい、配置を外して baseline へ戻した)。
  - 描画: `rubbleFaces`(天端が平らな土盛り=形そのものが「ここに立てる」の記号。h≥2 は天端に縁取り)/
    市街は `deco` ヒント(render-only・シムは読まない)で car=`wreckFaces` / curb=角張った割れた舗石 /
    slab=崩落塊+折れた鉄筋 に描き分け。レーダーは破線の等高線(h≥2 は二重)で壁の実線リングと区別。
    **機体は足場の高さぶん持ち上がる**(states に `cy`・`computeMechPose` の origin に `mech.elev` を加算。
    IK は origin から下へ解くので脚も接地点も一緒に上がる。game.js の interp で補間=段差を滑らかに登る)。
  - 検証(すべて緑。**クロスレビュー修正後に全部回し直した数値**): harness 本番6戦場/dev7戦場 ALL GREEN
    (dev 勝率 max63%/min31%・引分10.0%=既存と同水準)/
    **hazard-probe に踏破の判定ゲート7件を新設**し ALL GREEN(踏破0.24回/戦・乗る者と迂回する者の共存
    tank1.6s vs wheel0.0s・脚種で分かれる 1回あたり滞在 tank3.7s vs reverse1.4s・hover 0s・
    高所は遅い16.3 vs 21.7m/s・高所は露出する 回避13.4% vs 22.6%)/ gait-harness --dir dev --phys ALL GREEN /
    check-freeze OK(v4==live)/ harness-budget 本番・dev とも ALL GREEN /
    **スポーン実測4000シード×7戦場: 壁内スポーン0件・瓦礫上スポーン0件・フォールバック率は rubble 有無で完全一致**
    (deitan 0.90% / crater 1.45% は v4 からの既存値)/ 面生成プローブ 面57,588・頂点成分666,456で非有限0件 /
    実機 playwright: 1440px(崩落市街 戦闘完走)・390px(演習平原 arena テーマ 戦闘完走・崩落市街 戦闘中)
    いずれも **console エラー0**。
  - **フレーム間隔**(明示: **生成フレームを含まない**定常計測。自前の rAF チェーンで表示フレーム単位に計測):
    median 16.7ms / p99 17.5ms / max 17.7ms(419フレーム)=60fps 維持。
    **初回フレーム(テクスチャ生成を含む)を別途分離計測**: 新規レンダラの1回目 render が
    plain 89ms / shigai 107ms / haikyo 73ms、2回目は 0.4〜0.9ms。既知の未対応事項(下記参考①)を数値化した。
  - 実機で見つけて直した点: 歩道段差が丸い塚に見えて「路上の石ころ」に読めた(角張った5面+強い乱れの
    `chunky` を新設して割れた舗石にした)。
  - **⑤ 足元の高さを「乗る/沈む」の両方向へ**(人間指示 2026-08-01「脛が埋まるような高さの装飾で、
    硬い素材の物は乗り上げるようにして/沼は少し沈むようにして。動きが遅くなる理由がわかる」):
    - **沈む(シム側)**: 泥の中では `MUD_SINK×(1−泥係数)` だけ足元が下がる(車輪1.17m / 二脚・逆関節0.72m /
      履帯0.63m / 四脚0.54m / hover 0)。**脚を取られる脚種ほど深く沈む=なぜ遅いかが目で分かる**。
      当たり判定は不変(`climbExposure` が負の標高を0に丸めるので、沈んでも的は小さくならない)。
      ハーネス/プローブの数値が導入前と完全一致することで挙動不変を確認。
      states の `cy` を「足元の標高オフセット(+乗る/−沈む)」に一般化した。
    - **乗り上げる(描画側のみ)**: 縁石・折れた街灯/信号/電柱・散り瓦礫といった**硬くて脛が埋まる高さ**の
      装飾に足元を持ち上げる。乗って降りるだけで進路も速度も変わらないので**シムに上げる必要がない**
      (昇格の線引きを動かさずに「すり抜け」を消せる)。`fieldDecorFaces` が面と同じ場所で踏み面
      (円/カプセル・シムm座標)も出し、`decorLiftAt` が引く=装飾を動かしても踏み面が取り残されない。
      実測: 踏み面187面・高さ0.4〜1.8・可動域の6.4%で発生。0.09s でなまして跨ぐ動きにする。
    - 追加検証: 実機 1440px(崩落市街 コックピットHUD)・390px(泥炭湿地 arena テーマ 戦闘完走 WIN 43.0s)
      いずれも console エラー0、フレーム間隔 median 16.7ms / p99 17.6ms / max 17.7ms(生成フレームを含まない定常)。
      レビュー修正後に再実施: 1440px 演習平原(塚の乗り降り+近接戦)median 16.7 / max 18.7ms・
      崩落市街 arena 戦闘完走 1:09.7、いずれも console エラー0。
  - **⑥ クロスレビュー(Codex×Claude 並列)で直した11件**(2026-08-01。**重複ゼロ**・Codex が Claude の
    見落としを1件拾った。前回 St4 のときと同じ相補性が再現した):
    - ⑴ **足元の高さが二重に足されていた**(Claude): `cy`(シムの足場)と `decorLift`(装飾の踏み面)は
      どちらも「地面の高さ」なのに加算していた。市街の縁石(rubble h=1.1)の上に折れた街灯の踏み面
      (h=1.6)が重なる場所で機体が **2.2〜2.7 浮く**。**高いほう**を採る形へ。ただし単純な max だと
      装飾の無い所(0)が泥の沈み込み(負)を潰すので「装飾があるときだけ max」にする。
    - ⑵ **ハーネスの回帰**(**Codex**): hazard-probe に踏破の計測を足したとき、既存の
      「試合終了時にまだ地帯の中に居たエピソードを閉じる」2行を落としていた。**泥に入ったまま決着した
      試合の横断が丸ごと未計上**になっていた(修正で deitan の突入 430→470戦・biped の泥突入 1.21→1.51回)。
    - ⑶ **判定円と描画の形が食い違い、乗っているサンプルの 55.4% が斜面の上に浮いていた**(Claude):
      シムは円全体で持ち上げるのに、描いた塚の平らな天端は半径の 62% しかなかった。
      **人間判断でシム側を直す**=標高を縁に向けて落とすランプにした。天端の割合 `CLIMB_TOP_FRAC` を
      fields.js の正本にし、r3d.js が唯一の import でそれを読む(数値を写すと必ずいつか食い違うため)。
      副産物として **踏破の重さは円全体・高いのは真ん中だけ**になり、「丘の頂上に立った者だけが露出と
      見晴らしを引き受ける」という設計思想に形が寄った。
    - ⑷ **「踏破」電文が最大24行/戦**(Claude): 縁を出入りするたび再発火していた。ヒステリシス
      (1.5で入り1.0で抜ける)+3秒クールダウンで **最大9行/戦**へ。
    - ⑸ **共存ゲートが恒真**(Claude): `max >= min*2` は min=0 のとき常に真で、**全脚種が0(誰も乗らない)
      でも通っていた**。「よく乗る脚が1.0s/戦以上」かつ「避ける脚はその半分以下」の二段に。
    - ⑹ **shigai の判定円が1組重なっていた**(Claude): 廃車(556,900,r9)と縁石(571,900,r11)が距離15<20。
      自分で書いた「重ねない」不変条件に反していた → 縁石を (571,940) へ。
    - ⑺ **弾道・炸裂が標高に追従しない**(Claude): マズルフラッシュは computeMechPose 経由で上がるのに
      弾道と着弾は地上高のままで、塚の上から撃つと最大3ユニット(機体全高の7割)割れていた。
      shots に `ey0/ey1`、blasts に `ey` を足し、**イベント時刻の states の cy** を引く
      (2.4秒前のイベントを今の高さで描くと外れる)。炸裂は種類ごとに y の起点が散っているので、
      一旦受けてから頂点をまとめて上げる形にした(各所に足して回ると必ず取りこぼす)。
    - ⑻ **なましが実時計だった**(Claude): 倍速再生・一時停止・ヒットストップに追従しない。
      試合の時計へ。ついでに **cy 側にもなましを掛けた**(人間判断。装飾側と同じ 0.09s でテンポを揃える)。
    - ⑼ **敗者カメラに elev 未伝搬**(Claude): 瓦礫の上で撃破されると狙点だけ地面に残っていた。
    - ⑽ **昇格手順に dev の replay.js 同期が抜けていた**(Claude)→ 上記 St4 の昇格手順⑷へ明記。
    - ⑾ radar.js のヘッダ契約が `wall|mud|spike` のままだった(Claude)。
    - **問題なしと実測で確認された点**: 決定論(新規コードに rng 呼出0)/「泥の沈み込みは挙動不変」は
      504戦の全サンプル `(x,y,hp,en)` 完全一致で実証 / spawn 緩和は rng ストリーム不変 /
      新 kind の消費漏れなし・`obs_hit` の idx 契約不変 / `decorLiftAt` のキャッシュと座標系 /
      `battle.groundLift` は startBattle で再初期化 / 性能。
    - **再レビュー(修正箇所に絞って Codex×Claude をもう一巡)で追加で直した4件**: Codex は回帰なし。
      Claude が **自分の修正⑺の取りこぼし**を発見: 弾道の高さを引く関数が `cy` だけを見ていて
      **装飾の踏み面に乗って撃つと同じ割れが残っていた**(崩落市街のイベントの16.2%・最大1.80=
      機体全高の43%)。足元とまったく同じ式(`dl>0 ? max(cy,dl) : cy`)へ統一。
      併せて ⓐ 足元の更新を描画ブロックの外へ出した(レーダー/実況タブのまま決着すると狙点が
      地面に取り残されていた+敗者カメラの1フレーム遅れも解消) ⓑ 初回フレームは種を置く
      (`?still=1` は1フレームしか回らず、なますと目標の15%で固まっていた) ⓒ 電文側の
      `e.h >= 1.5` はシム側の門と二重になって死んでいたので撤去 ⓓ 昇格手順に
      「⑴と⑵は同一コミット」を明記(v5 の r3d.js は fields.js の CLIMB_TOP_FRAC を import するので、
      r3d.js だけ先に本番へ入って push されると **ESM リンクエラーで本番が白画面**になる)。
    - **参考(未対応)**: ①廃車の判定円 r=9 に対し車体の描画が小さく、ランプ後も真横4mで約1.3持ち上がる
      (最悪 2.4→1.3 に緩和済み)。②`r3d-three.js` の obsCache キーが `kind:x:y:r` なので、将来
      同座標・同半径で h だけ違う瓦礫を置くと古いメッシュを再利用する。③場外ビルの最接近が可動域から
      3.9m しかなく、`fieldDecorFaces` の配置式を触ると「装飾は膝下」の不変条件が黙って破れる
      (矩形判定での構造的ガードが要る)。④高さの閾値 2(radar/r3d)と 1.5(sim の電文)が散っている。
  - **ホバーのEN是非(人間問い 2026-08-01「これでホバーが強くなるね。必要エネルギーを増やす?」)**:
    同一seed・同一戦場で rubble の有無だけを変えて実測したところ **hover は +0.8pt(34.2%→35.0%)で
    脚種中いちばん弱いまま**(最大の伸びは quad +5.0pt / reverse +1.6pt、biped −1.1pt・wheel −1.6pt)。
    ホバーは drain 5.2〜6.0=全脚種最大で、機動の対価を既に EN で払っている。
    → **今回は据え置き。装備毎のバランス調整は別テーマへ送る(人間判断 2026-08-01)**=下記の申し送りへ。
  - **参考(未対応)**: ⑴撃破された機体が瓦礫の上で倒れるとき、横倒しの胴は足場の高さに追従するが
    ラグドールは無いので「天端に横たわる」だけ。⑵`climb` イベントを出しているが実況(voice.js)は
    まだ拾っていない(ログ電文には [踏破] 行を追加済み)。⑶`harness.mjs` の N=20 は戦場数7の倍数でないため
    戦場ごとの試合数に偏りが残る(既存の申し送り④と同じ)。
- [x] **観戦カメラ3点の直し(小障害物へのめり込み・決着後の周回・ワイドの戦果パネル位置)**
  (人間報告 2026-08-01「小障害物の中にカメラが入ってしまう」「決着後の周回が行われない事がある?」
  「ワイド表示時に決着後の敗者とダイアログが被ってて敗者が見えない」・**public/dev に実装 2026-08-01**・
  人間レビュー待ち)。**すべて描画/演出のみ=シム非改変(REPLAY_V 据置。St4+小障害物と同じ v5 に同梱可)**。
  - **⑴ カメラが瓦礫の中に入る**: 至近ショット(白兵 eye=0.84m/決着カット1.02m/肩越し2.88m/POV2.4m)が
    **足場の標高を見ていなかった**。瓦礫塚は世界半径9〜15mで高さ0.9〜3.2mあるので、塚の上の戦闘は
    カメラがまるごと土の中に入る。実測(演習平原の中央の塚 h=3.2 の上で殴り合う2機・1200ショット):
    **修正前は 1200/1200 が天端より下**(最悪 2.36m めり込み)→ **修正後 0/1200**(最小クリアランス0.84m)。
    直しは2段構え: ⓐ機体に紐づくショット(POV・肩越し・白兵・決着カット)を `elev` 込みの高さにする
    (`framePoints` の包含判定も y=0 固定をやめた=塚の上の機体が上端からはみ出していた)。
    ⓑ最後の砦として **表示カメラそのものに足場のクランプ**(r3d.js `cameraFloorAt`=踏破可能な瓦礫の天端+
    装飾の踏み面 / r3d-three.js が平滑化と包含の**後**に適用)。目標だけ直してもカット間の補間で塚を突っ切るため。
    せり上がりは0.18sでなまし、めり込み止めは生の天端で必ず効かせる二重の下限。
    壁は既存の近接カット透過が受け持ち、茨の棘(半径0.3m)は画面を覆わないので対象外。
  - **⑵ 決着後の周回が行われない**: 2つの原因の合わせ技だった。
    ⓐ **接近が遅すぎた**: `SPD` を毎フレーム残り距離から出していたため、近づくほど遅くなる。
    コメントは「遠距離決着でも8秒程度」だったが**実現していなかった**——2042戦の実測で決着時の機体間距離は
    p50=249単位あり、**周回開始まで p50 12.5s / p90 14.8s / 最悪21.0s**。その前に画面を閉じられていた。
    開始時の距離から**一度だけ**速度を決める形へ(到達5秒・上限46=標準二脚top31の1.5倍)。
    → **p50 4.9s / p90 6.6s、8秒以内に周回開始が 31%→93%**。
    ⓑ **カメラが旋回円の内側でラッチしていた**: 勝者は敗者の周りを半径10.4m(R2=23×WORLD_SCALE)で回るのに、
    余韻カメラの距離は `3.5+dist/2`=**10.3m**にしかならず(実測)、カメラが旋回円の上に乗っていた。
    ラッチ(amLock)がその構図を固定するので、勝者は周回の**半分以上を画面外**で過ごす
    (幾何: camDist≈旋回半径のとき見える範囲は ±82°=約46%)。`min(dist,12)×1.7` を距離の下限に足して
    **17.6〜20.4m**(近距離決着で17.6/遠距離決着はEMAが14を割った瞬間にラッチするので20.4)へ。
    実測で **周回中に勝者が画面内にいる割合 46%→88%**。敗者の見かけの大きさは従来の約半分になる。
    ⓒ **camSt(カメラの記憶)が試合をまたいで残っていた**(クロスレビューで発覚。**これが人間報告の
    「事がある」の正体**): レンダラは1ページ1インスタンスで camSt も1個。`reset` が捨てていたのは
    lineSide と povIdx だけで、**amLock を捨てていなかった**。そのため**2戦目以降は決着した瞬間に
    1戦目の座標へカメラが固定され、敗者も勝者も画面に入らない**(1戦目だけ見ていると再現しない=
    今回の初回調査で再現しなかった理由)。seed も残っていて、2戦目のショット割りが1戦目の初期位置由来に
    なっていた(「同じリプレイコードなら同じ画」が何戦目に見たかで崩れる)。reset で
    seed/distEMA/amLock/amAng/tripod/proj/lineSide/pov をすべて捨てる形へ。
  - **⑶ ワイド(コックピットHUD)の戦果パネルが敗者を隠す**: 余韻カメラは敗者を画面中心に据えるので、
    画面中央のパネルは**必ず**敗者に被る。左中段(TGT-Aとレーダー PiP の間)へ移し幅も 440→360px に。
    高さ760px以下では中段に置けない(`.cockpit` は幅1100pxだけの条件なので、縦の詰まったノート窓では
    レーダーPiPと重なり、`pointer-events:auto` のパネルがレーダー操作まで食う)→ その帯だけ
    レーダー上端を基準に積む。実機確認: 1440×860(中段)・1280×720 と 1120×640(レーダーの上・間隔12px固定)。
    非コックピット幅は右カラムに積まれる別レイアウトなので影響なし。
  - **クロスレビュー(Codex×Claude 並列)で直した8件**(2026-08-01。**重複1件**=Codex が指摘した
    「撃破決着でない敗者に横倒し姿勢を渡している」は Claude も参考段で拾っていた):
    ⑴ 上記⑵ⓒ **camSt の試合またぎ**(Claude。今回の3件のうち最も重い実害)。
    ⑵ **カメラの床が「天端一枚板」でシム/描画のランプと食い違っていた**(Claude): 円内一律 `o.h` にすると
    塚の縁を跨ぐ瞬間にカメラが最大3.2m跳ね、塚の**外**に立つ2機の白兵まで見下ろしになる。
    `sim.js` と同じランプ式(`CLIMB_TOP_FRAC` は fields.js の正本を import)へ統一。
    ⑶ **床0(素の地面)でもクリアランス1.2mが効いていた**(Claude): 瓦礫が1つも無い局面でも白兵至近が
    0.84→1.20に持ち上がり、**低い煽りが全戦場で失われていた**。足場の上でだけ効かせる形へ
    (なましの尾は0.02で切る=平地にクリアランスが残り続けない)。
    ⑷ **POVの照準リングが敵機からずれる**(Claude): shotPOV の target だけ標高込みにして、
    リング側(`r3d-three.js`)は地上高のままだった。頭上の敵機マーカーも併せて標高込みに。
    ⑸ **周回中の歩容が接近速度で回っていた**(Claude): `walk` の位相だけ `SPD`(最大46)のままで、
    実移動12.65の3.6倍で脚が空転していた(フットスケート)。`wspd` へ統一。
    ⑹ **撃破決着でない敗者に横倒し姿勢を渡していた**(Codex・Claude): 時間切れ判定/降参では敗者は
    立ったまま描かれるのに `alive:false, deadAge:99` 固定で、周回中心が余韻カメラの狙点とズレていた
    (既存不具合。`battle.loserDestroyed` で分岐)。
    ⑺ 戦果パネルとレーダーPiPの余裕が6pxしかなかった(Claude)→ 上記⑶の高さ760px以下の分岐。
    ⑻ 申し送りの「17.2mへ」が近距離決着だけの値だった(Claude)→ レンジ表記に訂正。
    - **問題なしと確認された点**: 決定論(新規コードに rng/実時計の持ち込み0)・`public/`(本番)未変更・
      REPLAY_V 据置が正しいこと・性能(`cameraFloorAt` は1フレーム1回、装飾の踏み面は shigai=187件/他0件)。
  - **再レビュー(修正箇所に絞ってもう一巡)で追加で直した7件**(Codex は「回帰なし」。Claude が
    **自分の修正が作った新しい段差**を2つ見つけた):
    ⑴ **床クランプの「足場の上だけ効かせる」ゲートが1.24mの段差を作っていた**: 床が0を超えた瞬間に
    +クリアランスぶん跳ねる=ランプ(⑵)で潰した縁の段差がクリアランス側から戻っていた。
    浮かせ量も床の高さに比例で立ち上げる形へ。**比例係数は床の傾きの増幅率でもある**ので 3 では
    4倍速でせり上がって跳ねた(ハーネスで実測 0.202/frame)→ 1 に(0.101/frame)。
    ⑵ **クリアランス1.2mが至近ショットのアイレベル(白兵0.84)より高かった**: 瓦礫の上では常に
    クランプが働き、**塚の上の殴り合いから低い煽りが必ず失われる**状態だった → 0.6 へ
    (「浮かせ量 < 1.4×MECH_SCALE」をハーネスの assert にした)。
    ⑶ **試合開始時にカメラを明示リセットしていなかった**: director の reset は「時計の逆行」検出なので、
    **レーダー/実況タブのまま次の試合を始めると3Dが1フレームも回らず逆行を見逃す**=⑵ⓒの再発経路が
    幅1100px未満の窓に残っていた。`r3d.resetCamera()` を startBattle から呼ぶ形へ。
    ⑷ 戦果パネルの分岐が760pxでは足りない: 戦果の行数(負傷/KIA/墓場)でパネル高が204→300px級に
    変わり、最悪ケースの交点は H≈804 → 境界を820pxに。
    ⑸ 床クランプの実装がレンダラ側にあり定数が二重管理だった → `cameraFloorClamp` として r3d.js へ
    (ハーネスも同じ関数を呼ぶ=「検査と実装が同じ量」の掟)。
    ⑹ ハーネスの穴(Claude): ⓐ`--dir` 省略で本番を見て異常終了(既定を dev に+未昇格ならスキップ)
    ⓑ2機を8単位しか離しておらず**全ショットが肩越しにフォールバック**していた(間合いを 8/70/150 で
    振り、種別の内訳を出して「肩越しだけに偏っていない」を assert)ⓒdirector の生出力を見ていて
    表示カメラ(平滑化+クランプ後)を検査していなかった ⓓ`camSt.seed` を渡していたが reset で毎回
    捨てられる死にコードだった(機体位置を振ってシードを変える形へ)。
    ⑺ 「描画とまったく同じ形」は言い過ぎ(rubbleFaces は頂点ジッタ±0.12r・chunky±0.25r、
    廃車は箱)→ 「シムと同じ形/描画はジッタぶん外れうる」に訂正。
  - **参考(未対応)**: ⑴周回の勝者が画面左端に来る間はパネルに一部重なる(画角±41°に対し周回は±37°まで
    振れるので、左右どちらに置いても一定割合は重なる。敗者を常時見せる方を優先した)。
    ⑵引き分け(実測2.8%)は勝者がいないので周回そのものが無い=仕様。
    ⑶高さ640px前後の窓では戦果パネルがTGT-Aと24px重なる(レーダーとの重なりは解消済み)。
    ⑷**自分が勝つ試合で降参すると余韻が反転する**(既存): `myWin=false` から勝者=相手 と決めるが、
    最終状態ではその相手が撃破済み=**横倒しの残骸が周回し、決着カットも敗者ではなく勝者側を向く**。
    今回の `loserDestroyed` 分岐は「立ったままの敗者」は救うが、この反転までは救っていない。
    ⑹`mechFocus` に渡す x/y はシム単位だが横倒しオフセットは描画単位(既存・約2シム単位のズレ)。
    ⑺POV も床クランプの対象(eye は自機の1.3m前方を見るので、急斜面では一人称だけ最大0.9m浮きうる)。
    照準リングは狙点に置くので画面中心からはズレない=実害は「目線が少し高い」だけと判断して据え置き。
    ⑻ハーネス C はシムの標高関数そのものではなく「同じ定数で書き直した式」と比べている
    (sim.js が式を変えても緑のまま=将来 sim 側の関数を export して呼ぶのが本筋)。
  - **人間判断で今回やった2件**(2026-08-01。レビューで「要判断」に分類したもの):
    ⑴ **余韻中の勝者の足場標高が決着時の位置で凍っていた**(既存不具合)→ **フレームループの順序を
    入れ替え**、余韻の移動を「足元の高さ・歩容・弾道」より前に置いた。これで以降のブロックが勝者の
    実位置を見る。加えて `cy`(シムの足場)は凍結値のままでは塚に追従しないので、
    **fields.js に `climbLiftAt(field,x,y)` を新設**(シムの移動処理と同じランプ式=天端まで平ら→縁で0)
    し、余韻中の勝者だけこれで取り直す。実機実測: 周回経路上に塚(r24/h3.0)を置くと
    足場が 3.00→2.5→0.09(縁)→0 とランプどおり追従(修正前は 0 のまま)。
    副産物として、勝者の移動が prevPos の差分に出るようになったので **歩容の手当て(walk/moveLocal を
    手で与えるコード)を削除**できた(実測でも従来と同じ値: walk 2.78/s・moveLocal.mag 0.42)。
    泥の沈み込み(脚種依存=シムの状態が要る)は拾わないので、余韻で沼へ入っても沈まない。
    ⑵ **余韻カメラの距離下限を aspect 連動に**: 係数を `1.15/sin(水平半画角)`(上限2.2)から出す。
    16:9 は従来と同値(1.69)、3Dタブがほぼ正方形になる幅では 2.2 まで開く。上限は縦画面のため
    (幾何の要求どおり4倍超に引くと敗者が豆粒になる)。ハーネスで aspect 1.67/1.78/1.0/0.62 の
    4通りとも「旋回円の外・全周が画角内」を確認。
  - **3巡目のクロスレビューで直した4件**(Codex は2巡目・3巡目とも「回帰なし」。Claude が実測付きで):
    ⑴ **`climbLiftAt` がホバーを除外していなかった** → `footYAt(field,x,y,legsKind)` に作り直し、
    **シムの footY と同じ契約**(hover は乗りも沈みもしない・瓦礫優先・泥は脚種別の深さ)へ。
    直前の版では**試合中は塚を突っ切っていたホバー機が、余韻でだけ天端に乗り上げていた**
    (実測: ホバー勝利405件中30件=7.4%で周回円が瓦礫に掛かる)。ついでに泥も同じ式で拾うので、
    沈んだまま決着した勝者が余韻開始で最大1.17m浮き上がる副作用(実測2.6%)も消えた。
    ⑵ **余韻カメラの距離下限が機体間距離比例のままだった** → 旋回半径そのものから出す形へ。
    撃破のある決着では決着カットの1.9秒で距離EMAが旋回半径へ収束するので成立していたが、
    **撃破の無い決着(判定勝ち・降参)には猶予が無く、決着間合いのまま初回フレームでラッチ**する
    (実測: 判定勝ち4件中1件が camDist 10.2m<旋回半径10.35m=直そうとした構図そのもの)。
    併せて**旋回半径の正本を r3d.js に置き**(`AFTERMATH_ORBIT_R_SIM`)、game.js とハーネスがそれを読む。
    ⑶ 接近→周回の切替で P が半径へスナップする距離(最大3sim)が、歩容を位置差分に移したことで
    脚のつんのめりとして出るようになった → 抜ける閾値を R2+3 → R2+0.5 に詰めた。
    ⑷ ハーネスに hover 免疫・泥の沈み・判定決着の経路を追加(⑴⑵は cam-probe では検出できなかった穴)。
  - **4巡目で直した3件**(Codex は回帰なし。Claude が実測で):
    ⑴ **周回に入る瞬間、勝者が旋回円までワープしていた**(旧来からの射影。**実測で全決着の23.5%が
    「決着時点で既に旋回半径の内側」**・最大21.8単位=9.8m)。歩容を位置差分に移したことで、これが
    **1フレームで脚が0.8周期ぶん飛ぶ**として画に出るようになっていた。半径を状態(`P.r`)として持ち、
    接近と同じ速度で R2 へ寄せながら回る形へ(角速度は半径で割って対地速度を一定に保つ)。
    実機実測: 1フレームの移動 0.41単位(=速度×フレーム時間)・歩行位相 0.09/frame で連続。
    ⑵ `footYAt` の「高さ0の瓦礫はスキップ」が sim と違っていた(sim は乗っている扱いで泥に沈まない)
    → onRubble は立ててから高さだけスキップする形へ。現行データでは到達しないが、h を書き忘れた
    rubble を1件足した瞬間に静かに壊れる枝だった。
    ⑶ ハーネスの取りこぼし: `orbitStartSec` が旧閾値(R2+3)のまま/周回の「全周が画角に入る」を
    表示するだけで assert していなかった/泥の戦場が見つからない場合の undefined ガード。
    ⑷ **ハーネスが実装より強い保証を主張していた**(縦長画面): `orbitK` は上限2.2で意図的に頭打ち
    =幾何どおり(縦画面で4倍超)引くと敗者が豆粒になるので「全周が画角内」は設計上の保証ではない。
    にもかかわらず assert がそれを要求しており、実測では `containEye` の押し出し次第で緑にも赤にも
    なる状態だった(方位を10°刻みで振ると 36通り中15通りが NG)。上限が効かない帯だけ「全周」を
    要求し、効く帯は「旋回円の外+上限まで引いている」を見る形へ。
    ⑸ 縦画角(55°)が3箇所に写されていた(r3d.js・r3d-three.js・ハーネス)→ **r3d.js の `FOV_Y` を
    export して他の2つが読む**。写しのままだと画角を変えたときハーネスが「間違った値で緑」になる。
  - **ハーネス**: `tools/harness/cam-probe.mjs` を新設(`node tools/harness/cam-probe.mjs --dir dev`)。
    r3d.js は DOM 非依存で node から import できるので、①塚の上の全ショットでめり込みゼロ ②平地で床0
    ③塚の形がシムの標高式と一致 ④周回開始までの秒数(実戦1050戦の決着距離分布) ⑤余韻カメラが旋回円の外
    ⑥試合またぎで camSt が持ち越されない、を assert する。上の数値はすべてこれで再現できる。
- [ ] **対戦までの画面と遷移の再構成(UI/UX)** — 設計と段取りは [docs/ux-flow.md](ux-flow.md)(2026-08-02 作成・改訂)。
  **段1・段2 は public/dev に実装済み(人間レビュー待ち)・段3〜段6 は未着手。**
  決定事項: ⑴成果物は `docs/ux-flow.md` を新設(タスク追跡ではなく設計提案のため。人間了承の既定方針)。
  ⑵未完了だった「オンボーディング(初見導線)の作り込み」は本テーマに畳んだ(ux-flow.md §4.7)。
  ⑶提案は**全部プレゼン層=REPLAY_V 据置**(ui.js/style.css/game.js のみ。sim/fields/parts に触れない)。
  ⑷`S`(kouki_save_v4)は**キー追加のみ**に限れば非破壊(読込が `Object.assign(freshSave(), …)` のため)。
  ⑸URL クエリ(`?r=` `?auto=1` `?t=` `?field=` `?seed=` `?still=` 等)は共有・撮影・検証の契約につき変更しない。
  ⑹**⚠ 一連の UI/UX 改修が終わるまで本番(`public/`)は触らない — dev だけで進める(人間判断 2026-08-02)。**
  v5(St4+小障害物+観戦カメラ)の昇格も改修完了後に**まとめて1回**。したがって
  **`make-dev.mjs` を絶対に流さないこと**(本番から dev を再生成するので v5 一式も UI 改修も丸ごと消える)。
  最終昇格時はコピー対象に `ui.js` / `style.css` が加わる(手順の正本は下記 St4「昇格手順」)。
  初版で推していた「v5 を先に昇格させてから UI」は撤回(ux-flow.md §5 段0)。
  **人間判断 2026-08-02(§4 以降を統合版に改訂)**: ⑺**決着は画面遷移せずオーバーレイのまま**——モバイルは
  「下端固定の2段シート(畳=116px / 開=max 52vh)」で 3D の余韻を隠さず必ず視界に入れる(実測: 縦積みは
  常に fold 外・最悪306px、横持ち844×390 も129px 外、1024×600 は最悪KIAで53px 外)。
  ⑻**兵舎を新設**しパイロット/墓場を集約。**今回作るのは 作る/選ぶ/解雇/墓場 だけで「鍛える・引退」は後日**
  (器を先に作る)。⑼**練兵場で機体・パイロットまで決める**(`S` はキー追加不要)⑽**カタログは工廠へ吸収**
  ⑾**闘技場(大会・管理者画面)は別テーマ**=サーバ設計を含むため分離。順位表に出せるのは識別コードのみ
  (パイロット名は現在も端末外に出ていない)。管理UIは公開クライアントに置かない。
  ⑿**工廠=鑑賞モード**: 全画面ステージでパーツを選びながら動かす。**鑑賞用コピーを廃止し実ビルド直編集**
  (`viewerBuild`/`viewerStats`/`ui.viewerBuild()`/`onDeriveStats` を削除。`game.js` は既に `|| S.current` へ
  フォールバックする)。事故対策だった分離は「総額を常時表示+超過中は出撃を無効化」で置き換わる。
  副産物として **WebGL レンダラが3→2インスタンス**(既知の重複生成コストが1組減る)。
  段取りは1段=1画面: 段1 レイアウト手当て → 段2 工廠 → 段3 兵舎+常設ナビ → 段4 練兵場 → 段5 カタログ吸収+掃除 → 段6 初見導線。
  ⒀**決着の主ボタンは「▶ 次の相手」を採用**(人間判断 2026-08-02)。⒁**保存スロットの置き場所は設計者判断に一任**
  →固定バーからの呼び出し式(常設すると 394px がパーツ選びを押し下げるため。ux-flow.md §4.4)。
  - [x] **段1=非破壊のレイアウト手当て**(**public/dev に実装 2026-08-02**・人間レビュー待ち。ui.js/style.css/game.js。
    **シム非改変・REPLAY_V 据置・`S`/URLクエリ 不変**): ①決着シート(モバイル/低背は下端固定の2段シート。
    畳=116px でステージ下の空きに収まり **3D の余韻を1pxも隠さない** / 開=max min(52vh,440px)+内部スクロール。
    横並びの低い窓では全幅ではなく HUD 列 300px に重ねる=ステージに掛からない)②**「▶ 次の相手」を主ボタンに**
    (`startCampaignFight` を新設し出撃画面と決着で入口を共通化。行き先は `nextUnclearedFight()`=解放済みランクの
    最初の未クリア戦。負けたときはラベルが「▶ 挑み直す」。クリア済みは対象外なので報酬減衰の抜け道にならない。
    KIA 直後は出さない)③`showScreen` のスクロール先頭化 ④決着に「⚙ 工廠で直す」。
    **ワイドの本文幅 520px の緩和は段5のあとへ送った**(画面の顔ぶれが決まる前に決めると三度やり直すため)。
    **自分の変更が作った回帰を1件修正**: パネルが高くなり最悪ケースで 1440×860 のときレーダーPiPに 18px 食い込んだ
    → 上限を固定値でなく幾何から出す形へ(中段=`100dvh−2×下端障害物` / 積み直し=`−TGT-A ぶん215px`)+内部スクロール。
    ついでに旧申し送り「高さ640px前後で戦果パネルがTGT-Aと24px重なる」も解消。
    検証: 9サイズ(360×640〜1440×900)で収まり実測・最悪行数を注入してレーダー/TGT-A との余白12px以上・
    最長敵機名も収まる・`?auto=1` の result 経路不変・console エラー0・harness/cam-probe/gait-harness(dev) ALL GREEN・
    check-freeze OK(v4==live)。
  - [x] **段2=工廠 = 鑑賞モード**(**public/dev に実装 2026-08-02**・人間レビュー待ち。**シム非改変**):
    工廠と機体鑑賞を1画面に統合し、**鑑賞用コピーを廃止して実ビルド(`S.current`)直編集**にした。
    作業の主体は削除=`viewerBuild`/`viewerStats`/`viewerDerive`/`ui.viewerBuild()`/`onDeriveStats`/
    `previewTick`+工廠プレビュー用レンダラ。**画面 9→8**・**WebGL レンダラ 3→2**(`makeR3D` は工廠と戦闘だけ)。
    コピーが守っていた事故(高額構成に送って出撃不能)は「総額を下の帯に常時表示+超過中は出撃を無効化」が受け持つ。
    自動実演(旧プレビューの20秒巡回)は残し、移動/旋回/動作を触った瞬間に手動へ降りる。工廠に入ると
    必ず直立・停止・自動実演から。パーツはカード一覧と ◀▶ の両方から替えられる(書込は `applyPart` 1本)。
    保存スロットは呼び出し式シート(常設だと394pxがパーツ選びを押し下げる)。
    検証: **P2 が構造的に消えた**——カード先頭を画面上端に置いた状態(旧実装はゲージ0/7・総額はfoldの302px上)で
    390×844 で ステージ388px全見え/ゲージ6/6/総額+出撃ボタンも可視。直編集(lg1→lg2 が localStorage に追従・
    総額2,650→2,680C)・スロット往復(lg14→lg1→lg14)・導線(工廠→出撃→戦闘→決着→工廠で直す)・
    1440×900/390×844 実機・console エラー0・harness/cam-probe/gait-harness(dev) ALL GREEN・check-freeze OK。
  - [x] **段1+段2 のクロスレビュー(Codex×Claude 並列)で直した7件**(2026-08-02。**重複ゼロ**・
    Codex の1件は実測で否定。詳細は ux-flow.md「クロスレビューで直した7件」):
    ⑴**「次の相手」が上位ランクから最下位ランクへ引き戻していた**(最も実害大。`nextUnclearedFight()` が
    CAMPAIGN の先頭から探していた → 同ランク優先へ。文書化していた「負ければ挑み直す」も**一度も出ていなかった**。
    実データ総当たり+実機で再検証)⑵決着の「⚙ 工廠で直す」だけ `refreshCampaign()` を呼ばず、
    **勝ってランク解放されたのに 🔒 のまま**がリロードまで残っていた ⑶スロットシートが総額バーと出撃ボタンを
    完全に覆っていた(重なり92px・当たり判定も奪う=**コピー廃止の安全網が無効**)→ シートをバーの中へ
    ⑷**段1で入れた幾何式の回帰**: コックピットの `max-height` が低背窓(1280×450)で 0 に丸められ戦果が消える
    → 下限150px ⑸予算超過のまま工廠を離れられた → 離脱時に確認(人間判断 2026-08-02)
    ⑹くり返しボタンが自動実演中に無反応 ⑺docs の食い違い3件(状態表記・96/116px・§4.4の表)。
    **Codex 指摘「開いた決着シートの overflow が効かない」は実測で否定**(`.tgt-box` から flex を継承・
    14行注入しても 203px=52vh のまま内部スクロール)。修正後: harness/cam-probe/gait-harness(dev) ALL GREEN・
    check-freeze OK・`node --check` OK・console エラー0。
  - [x] **再レビュー(修正箇所に絞ってもう一巡)で直した4件+人間判断2件**(2026-08-02。詳細は ux-flow.md):
    ⑴**工廠の予算が古いまま固まっていた**(実害あり): タイトルの「⚙ 工廠」は `showScreen('hangar')` を
    呼ぶだけで `refreshHangar()` を通らず、**演習で稼いだクレジットが反映されない**。段2で予算が出撃の可否を
    決めるようになったため「予算内なのに出撃できない」に昇格していた → `onEnterHangar` フックを新設し
    **どの経路から入っても取り直す**(実測: 3,000→勝利→3,100C に追従)⑵**Codex**: `applyPart()` が
    `renderViewerActs()` を呼ばず、◀▶ で武装を替えても動作ボタンが前の武器名のままだった
    ⑶「🔁 くり返し」が最も自然な操作順で無反応(自動実演中に毎フレーム `vwLastAct` を潰していた)
    ⑷docs の残り4箇所(96px/遷移図/ボタン順/前提条件)。
    **Codex の「calc() 乗算が落ちる」は実測で否定**(`CSS.supports` 真・1440×860 で実効332px=幾何どおり)。
    **人間判断で「現状のまま」にした2件**: ⓐ「次の相手」の端のケース(最上位クリア後の巻き戻し・同ランク内の
    前の未クリア戦へ送る)は**「未クリアを順に埋めさせる」振る舞いとして仕様とする** ⓑスロットシート展開時に
    低い横持ち窓でステージの約6割を覆う点は許容(安全網は常時可視)。
    修正後: harness/cam-probe/gait-harness(dev) ALL GREEN・check-freeze OK・`node --check` OK・console エラー0。
- [ ] **装備・脚種ごとのバランス調整(EN収支の辻褄合わせ)**(人間指示 2026-08-01「装備毎のバランス調整は
  後でやることにします」で保留・**アイデア段階**): 小障害物を入れた時に出た論点。**シム改修=REPLAY_V が上がる**
  ので、まとまった一本のテーマとして着手する(小刻みにいじると版が増えるだけ)。
  - 出発点になった人間の設計観: 「**ホバーは移動に有利だけど、実弾系じゃないとエネルギー消費の辻褄が
    合わない**」= 機動を EN で買っている脚は、弾数制限のある実弾兵装と組ませて初めて収支が閉じる、という筋。
  - **現状はその筋の逆になっている**(2026-08-01 実測): sniper(ホバー+EN兵装1門)38.8% に対し
    skirmish(ホバー+実弾のみ)31.3%。狙いどおりなら実弾ホバーのほうが回るはずが、弱い。
  - ホバー自体は脚種中いちばん弱い(35.0%)。**drain(出力を削る)を上げるのは筋が悪い**——常時ジリ貧に
    なるだけで選択が生まれない。効くのは **enCap(容量)を削る**ほう: 「連射は続かないが、弾切れしない
    実弾なら回る」という選択がそこで初めて立つ。cap は `parts.js` の gn 側にあるので、
    脚に「cap 補正」を足すか、ホバー向けの低cap炉を新設するかの設計判断が要る。
  - 併せて見る指標: `tools/harness/harness.mjs`(総当たり勝率 max≤68/min≥25/引分≤12)と
    `harness-budget.mjs`(初期予算3000Cの多様性)。EN兵装/実弾の別で勝率を割って出す集計は未整備なので、
    **着手時にハーネスへ「兵装のEN依存度 × 脚種」のクロス集計を足す**ところから始めるのが早い
    (今回は使い捨てスクリプトで測った: EN収支 enOut・cap・武器の連続消費EN/s・EN兵装の門数)。
- [ ] **撃破ラグドールを物理で**(描画側): cannon-es をブラウザにも載せ撃破の崩れ落ちを物理ラグドール化。決定論安全だが
  ランタイムコスト増。演出の作り込み段で検討。
- [ ] **物理をシム本体に入れる**(大工事): 物理駆動の"結果"(実座標ノックバック/地形衝突が移動を決める等)が要る機能が出たら。
  クロス環境(browser V8/JSC × workerd)決定論のため **WASM決定論エンジン(Rapier enhanced-determinism 等)or 固定小数点**+
  固定タイムステップ+**エンジン版を REPLAY_V ごとに凍結**+「browser vs workerd 再現一致」ゲートが必須。純JSの cannon-es は不可。
  物理駆動の結果が要る機能が出るまで**やらない**(結論: LESSONS「3D/レンダラ移行」節)。

---

## kouki — St1 以前からの既存申し送り(未消化)
- [x] 戦場ハザードの設計(泥/棘などのギミック拡充)→ public/dev に実装済(2026-07-24)・
  **人間レビューOK(2026-07-29「動作確認OK」)**。次の一手=リリース段取り(下記)へ:
  ①sim.js にハザード認知AI(踏む意欲×踏む価値で「渡る/迂回」を決定論判断・rng不使用・振動防止3点)
  ②deitan=泥の大河・crater=茨の堡塁・新戦場 ibara「茨の回廊」(FIELD_CODES 末尾追記)
  ③hazard-probe.mjs でジレンマを数値実証(wheel 泥滞在31.7→2.2s・突入率83%・判定ゲート4件)。
  harness ALL GREEN(hover支配なし)・check-freeze OK・実機 console 0・Codex 冷読み3件採否済。
  **リリース完了(2026-07-29 commit 7d6a0d2)**: release-sim.mjs で REPLAY_V 3→4・sims/v4/ 凍結・
  replay.js FIELD_CODES に 'ibara' 追記(掟2)・St3 新パーツ9種を同じ v4 に同梱・
  workers/kouki(fable-kouki)再デプロイ(Version 3f77ddb2)。同コミットで描画/ゲーム側も本番昇格
  (r3d/r3d-three/game/ui/voice/voice-lines/style、DEV=false 戻し済)。昇格後検証すべて緑:
  check-freeze OK(v4==live)・harness(本番) ALL GREEN(勝率max64%/min28%)・gait-harness --dir . --phys
  ALL GREEN・旧v3リプレイコード互換1件実測(Node: decode→sims/v3 再生で states/勝者/ハッシュ完全一致+
  本番URLブラウザ再生OK)・v4コード往復(ibara+新パーツ)OK・本番URL実機(工廠/鑑賞/戦闘) console エラー0。
  詳細 making.md「■戦場ハザードのジレンマ設計」。
- [ ] オンボーディング(初見導線)の作り込み → **上の「対戦までの画面と遷移の再構成」に畳んだ**(2026-08-02。設計は [docs/ux-flow.md](ux-flow.md) §4.7)。
- [ ] OAuth の kodama・kouki 分離(**主管=このリポに一本化。2026-07-29 人間決定**): 現在 kouki は kodama
  (fable-playground)と同じ Google OAuth App+secret を共用しており、secret 再作成の巻き添え事故が既に1回
  ある(2026-07-07)。人間作業=Google Cloud Console で kouki 専用 App 作成(同意画面・redirect URI=
  `https://fable-kouki.d3j.workers.dev/auth/google/callback`)→ `wrangler secret put GOOGLE_CLIENT_SECRET`+
  wrangler.toml の GOOGLE_CLIENT_ID 差し替え。kodama 側(fable-playground/workers/kodama)の secret 差し替えも
  着手セッションが面倒を見る。着手時はユーザーと段取り確認。
